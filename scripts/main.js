console.log('Fun Memorization Site Loaded');

// Navigation logic can go here later
const init = () => {
    // Check for user preferences or existing session data
};

document.addEventListener('DOMContentLoaded', init);
